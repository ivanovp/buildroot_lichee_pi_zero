#!/bin/sh
DAEMON="modprobe"

start() {
	printf "Starting $DAEMON: "
	modprobe 8188eu && modprobe rtl8xxxu && \
	echo "OK" || echo "FAIL"
}

stop() {
	printf "Stopping $DAEMON: "
	echo "OK" || echo "FAIL"
}

case "$1" in
	start)
		start
		;;
	stop)
		stop
		;;
	restart|reload)
		stop
		sleep 1
		start
		;;
	*)
		echo "Usage: $0 {start|stop|restart}"
		exit 1
esac
